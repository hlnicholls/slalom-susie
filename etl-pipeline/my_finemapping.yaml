# Default config
defaults:
  - finemapping
# Additional config
study_locus_path: ${datasets.outputs}/catalog_study_locus
ld_matrix_template: ${datasets.ld_matrix_template}/gs://gcp-public-data--gnomad/release/2.1.1/ld/gnomad.genomes.r2.1.1.{POP}.common.ld.variant_indices.ht
filtered_study_locus_path: ${datasets.filtered_study_locus}
filtered_ld_matrix_path: ${datasets.filtered_ld_matrix}
finemapping_results: ${datasets.finemapping_results}
